import { Text } from '@chakra-ui/react';
import PageWrapper from '../../components/page-wrapper';

const Home = () => {
  return (
    <PageWrapper>
      <Text>Home, show top articles etc.</Text>
    </PageWrapper>
  );
};
export default Home;
